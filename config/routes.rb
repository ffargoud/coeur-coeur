Rails.application.routes.draw do
  # For details on the DSL available within this file, see http://guides.rubyonrails.org/routing.html

  # Read all the centres (all)
  # Read one centre (all)
  # Create a centre (admin)
  # Update a centre (admin)
  # Delete a centre (admin)

  # Read all slots (all) ?
  # Read one slot (all)
  # Create one slot (admin)

  # Read all users (admin)
  # Read one user (admin & user)
  # Create a user (admin)
  # Udpate a user (admin & user)
  # Delete a user (admin)

end
